"use strict";
const mediaConfig = {
  /*
  |--------------------------------------------------------------------------
  | Media config
  |--------------------------------------------------------------------------
  */

  main_upload_dir: "/public/uploads",

  imageUpload: {
    ImagePath: "blogs/",
  },
};

module.exports = mediaConfig;
